# seat

A Vite + React + TypeScript starter project.

## Getting Started

1. Install Node.js in WSL if it is not already available.
2. Install dependencies:

```bash
npm install
```

3. Start the dev server:

```bash
npm run dev
```
